name: SonarCloud analysis

on: 
  workflow_run:
    workflows: [CI test report]
    types: [completed]

jobs:
  sonarqube:
    runs-on: ubuntu-latest
    if: github.event.workflow_run.conclusion == 'success'
    steps:

    - name: echo event
      run: cat $GITHUB_EVENT_PATH

    - name: Download PR number artifact
      id: download_pr_number
      if: github.event.workflow_run.event == 'pull_request'
      continue-on-error: true
      uses: dawidd6/action-download-artifact@v9
      with:
        workflow: CI test report
        run_id: ${{ github.event.workflow_run.id }}
        name: PR_NUMBER

    - name: Read PR_NUMBER.txt
      if: github.event.workflow_run.event == 'pull_request' && steps.download_pr_number.outcome == 'success'
      id: pr_number
      uses: juliangruber/read-file-action@v1.1.7
      with:
        path: ./PR_NUMBER.txt

    - name: Request GitHub API for PR data
      if: github.event.workflow_run.event == 'pull_request' && steps.download_pr_number.outcome == 'success'
      uses: octokit/request-action@v2.x
      id: get_pr_data
      with:
        route: GET /repos/{full_name}/pulls/{number}
        number: ${{ steps.pr_number.outputs.content }}
        full_name: ${{ github.event.repository.full_name }}
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

    - name: Checkout repo
      uses: actions/checkout@v4
      with:
          repository: ${{ github.event.workflow_run.head_repository.full_name }}
          ref: ${{ github.event.workflow_run.head_branch }}
          fetch-depth: 0

    - name: Checkout base branch
      if: github.event.workflow_run.event == 'pull_request'
      run: |
        git remote add upstream ${{ github.event.repository.clone_url }}
        git fetch upstream
        git checkout -B ${{ fromJson(steps.get_pr_data.outputs.data).base.ref }} upstream/${{ fromJson(steps.get_pr_data.outputs.data).base.ref }}
        git checkout ${{ github.event.workflow_run.head_branch }}
        git clean -ffdx && git reset --hard HEAD

    - name: Download coverage artifact
      id: download_coverage
      continue-on-error: true
      uses: dawidd6/action-download-artifact@v9
      with:
        workflow: CI test report
        run_id: ${{ github.event.workflow_run.id }}
        name: coverage-report

    - name: Unzip coverage
      if: steps.download_coverage.outcome == 'success'
      run: unzip -o coverage-report.zip
        
    - name: Fix Go module paths in coverage
      if: steps.download_coverage.outcome == 'success'
      run: |
        sed -i 's|github.com/nephio-project/porch|.|g' coverage.out
        
    - name: SonarQube Scan on PR
      if: github.event.workflow_run.event == 'pull_request' && steps.download_coverage.outcome == 'success'
      uses: SonarSource/sonarqube-scan-action@v7.0.0
      env:
        SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
      with:
        args:
          -Dsonar.projectKey=nephio-project_porch
          -Dsonar.organization=nephio-project
          -Dproject.settings=sonar-project.properties
          -Dsonar.pullrequest.key=${{ fromJson(steps.get_pr_data.outputs.data).number }} 
          -Dsonar.pullrequest.branch=${{ fromJson(steps.get_pr_data.outputs.data).head.ref }} 
          -Dsonar.pullrequest.base=${{ fromJson(steps.get_pr_data.outputs.data).base.ref }}

    - name: SonarCloud Scan on push
      if: github.event.workflow_run.event == 'push' && github.event.workflow_run.head_repository.full_name == github.event.repository.full_name && steps.download_coverage.outcome == 'success'
      uses: SonarSource/sonarqube-scan-action@v7.0.0
      env:
        SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
      with:
        args:
          -Dsonar.projectKey=nephio-project_porch
          -Dsonar.organization=nephio-project
          -Dproject.settings=sonar-project.properties
